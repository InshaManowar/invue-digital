[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# SPA fallback so client-side routes (e.g. /contact) don't 404 on direct load/refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
